# JWT Configuration
JWT_SECRET=routeAtt
JWT_EXPIRES_IN=5s  # Optional: To centralize token expiration settings

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/RouteAttendence
MONGO_OPTIONS=?retryWrites=true&w=majority  # Optional for advanced MongoDB setups

# Server Configuration
PORT=5000  # Optional: To allow changing the port easily

# Debugging
NODE_ENV=development  # Optional: Can switch to "production" later
